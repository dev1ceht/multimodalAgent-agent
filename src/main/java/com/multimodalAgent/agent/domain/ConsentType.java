package com.multimodalAgent.agent.domain;

/** Versioned consent purposes that can be independently granted or revoked. */
public enum ConsentType {
    PRIVACY_NOTICE,
    SENSITIVE_DATA_PROCESSING,
    RISK_MONITORING
}
