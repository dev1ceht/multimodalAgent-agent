# 将知识增强路由与心理风险等级解耦

请求路由采用 `needsRag: boolean` 与 `riskLevel: NONE|LOW|MEDIUM|HIGH` 两个正交字段，不再由 `CHAT/CONSULT/RISK` 单标签同时决定知识检索、心理评估、报告和安全处置。纯心理支持知识问答可以使用 RAG 而不把提问者标记为有心理风险；中高风险仍强制使用 RAG，高风险词和多模态中高风险信号只能提高最终风险等级。我们接受路由 JSON 校验、数据迁移和评测改造成本，以换取更清晰的产品语义、更少的心理化误判和可分别诊断的 RAG/安全指标。
